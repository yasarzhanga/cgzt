# AGENTS.md

本文件用于指导后续 Codex 在本仓库中修改“画法几何与机械制图自动批改 Unity版”项目。

## 1. 项目概况

- 本项目是 Unity/C# 项目，主要用于机械制图、画法几何相关内容的自动批改、显示、交互和数据处理。
- 主要代码位于 `Assets` 目录下，尤其是各类 C# 脚本。
- `Packages` 和 `ProjectSettings` 是 Unity 项目配置目录，应谨慎修改。
- `README_GIT.md` 已说明本仓库偏向代码级版本控制，资源、大型二进制、运行时数据默认不纳入 Git 管理。

## 2. 修改原则

- 做最小必要修改，避免无关重构。
- 优先修改 C# 脚本，避免无必要修改场景、Prefab、`ProjectSettings`。
- 保持现有命名、目录结构和代码风格。
- 新增逻辑要清晰、可维护；复杂或容易误解的位置可以添加简短中文注释。
- 不随意引入第三方依赖。
- 不随意添加 DLL、模型、图片、字体、大型二进制文件。

## 3. Unity 相关规则

- 新增或移动 Unity 资源时，注意对应 `.meta` 文件，避免破坏 Unity 资源引用。
- 不提交 `Library`、`Temp`、`Logs`、`Builds`、`UserSettings` 等 Unity 生成目录。
- 不提交学生答案、运行时保存数据或临时数据，例如 `Assets/StreamingAssets` 和 `Assets/save` 下的运行时数据。
- 修改 Editor 脚本、Runtime 脚本、Shader 时，要注意 Unity 生命周期、程序集边界和平台兼容性。

## 4. 验证要求

- 修改后执行 `git status` 和 `git diff`，确认只包含任务相关改动。
- 如环境支持，运行 Unity 编译、测试或相关检查。
- 如环境不支持 Unity，请说明无法运行原因，并做静态检查。
- 最终报告要列出修改文件、验证命令、风险点和需要人工在 Unity 编辑器确认的事项。

## 5. 工作流程

- 对复杂任务，先阅读相关代码并给出简短计划。
- 不确定需求时，优先做保守、可回滚的小改动。
- 不要改动与任务无关的文件。
- 完成后自查 diff，避免格式化或重排大量无关代码。
